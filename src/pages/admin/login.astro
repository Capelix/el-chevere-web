---
import MainTitle from '@/components/MainTitle.astro'
import SectionContainer from '@/components/SectionContainer.astro'
import { LoginForm } from '@/components/sections/LoginForm'
import { Login as LoginIcon } from '@/icons/Login'
import Layout from '@/layouts/Layout.astro'
import { getI18N } from '@/locales/index'

const currentLocale = Astro.currentLocale
const i18N = getI18N({ currentLocale })
---

<Layout
	title=`${i18N.LOGIN} - Foto Estudio El Chévere - Official Web`
	description='Login page'
	canonical='https://fotoestudioelchevere.com/login'
>
	<SectionContainer>
		<div class='flex w-full max-w-6xl flex-col md:w-11/12 lg:w-10/12'>
			<MainTitle>
				<LoginIcon classes='size-8 text-accent' />
				{i18N.LOGIN}
			</MainTitle>

			<LoginForm client:visible />
		</div>
	</SectionContainer>
</Layout>
